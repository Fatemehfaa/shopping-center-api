spring:
  application:
    name: shoppingCenter
  datasource:
    url: jdbc:mysql://localhost:3306/shoppingCenter
    username: root
    password: 123456
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update